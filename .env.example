# Optional: override the default public Robinhood Chain RPC
NEXT_PUBLIC_RPC_URL=https://rpc.mainnet.chain.robinhood.com
# Optional fallback RPC used when the primary is unhealthy
RPC_FALLBACK_URL=https://robinhood-rpc.publicnode.com
